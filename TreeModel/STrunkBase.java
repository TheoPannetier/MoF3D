import de.grogra.graph.impl.Node;

public class STrunkBase extends SBranch
{
    public STrunkBase()
    {
		super();
    }
    
    public STrunkBase(int treeId, int axisId, float length, float diameter, int order)
    {
		super(treeId,axisId,length,diameter,order);
    }
}